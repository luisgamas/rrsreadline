[package]
name = "rrsreadline"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Cross-platform shell history suggestions"
readme = "README.md"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
pretty_assertions = "1"
